{
  "name": "graph-editor-pro",
  "version": "2.0.0",
  "private": true,
  "description": "Graph Editor Pro — single-file offline graph editor built from TypeScript sources",
  "license": "AGPL-3.0-only",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "build": "node scripts/build.mjs",
    "test": "node scripts/smoke.test.mjs",
    "all": "npm run check && npm run build && npm run test"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "jsdom": "^25.0.0"
  }
}
